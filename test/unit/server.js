
  // it('should gen unique client id', () => {
  //   var clientId = TunnlServer.genClientId();
  //   log.info('generated clientId:', clientId);
  //   expect(clientId.length).to.equal(8);
  // });

  // it('should create client', (done) => {
  //   var clientId = TunnlServer.genClientId();
  //   TunnlServer.createClient(clientId, (err, data) => {
  //     expect(err).to.not.exist;
  //     expect(TunnlServer.stats.tunnels).to.be.greaterThan(0);
  //     log.info(data);
  //     // setTimeout(() => {
  //     // expect(TunnlServer.stats.tunnels).to.be.equal(0);
  //     done();
  //     // }, 5500);
  //   });
  // });
